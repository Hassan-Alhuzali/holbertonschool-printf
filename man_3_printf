.TH _PRINTF 3 "2024" "Holberton School" "Printf Project"

.SH NAME
_printf - custom printf function

.SH SYNOPSIS
int _printf(const char *format, ...);

.SH DESCRIPTION
Produces output according to a format string.
The format string is composed of zero or more directives.

.SH CONVERSION SPECIFIERS
.TP
.B %c
Prints a character.
.TP
.B %s
Prints a string. If the string is NULL, prints (null).
.TP
.B %%
Prints a percent sign.
.TP
.B %d, %i
Prints a decimal integer.

.SH RETURN VALUE
Returns the number of characters printed.
Returns -1 if format is NULL.

.SH AUTHORS
Wasaal Alquaydhib and Hassan Alhuzali
